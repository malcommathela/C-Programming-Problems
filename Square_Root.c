#include <stdio.h>
#include <math.h>

int main(){
    float number,result;
    printf("Enter Number:\n");
    scanf("%f",&number);
    result = sqrt(number);
    printf("Result: %f",result);
    return 0;
}